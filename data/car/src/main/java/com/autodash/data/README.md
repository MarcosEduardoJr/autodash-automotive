# `data/car/src/main/java/com/autodash/data/`

Segmento de **pacote** (`data`). Convenção Android: pacote = namespace reverse-domain; este nível só agrupa os pacotes-filhos (o código está nas folhas).
