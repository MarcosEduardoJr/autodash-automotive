# `data/car/src/main/java/com/autodash/`

Segmento de **pacote** (`autodash`). Convenção Android: pacote = namespace reverse-domain; este nível só agrupa os pacotes-filhos (o código está nas folhas).
