# `gradle/wrapper/`

O **Gradle Wrapper**: `gradlew`/`gradlew.bat` + `gradle-wrapper.jar` + `.properties`
fixam a versão do Gradle (8.9) para todos. Rode `./gradlew ...` sem instalar nada.
Commitar o wrapper é padrão — garante build reprodutível no time e no CI (docs/08).
