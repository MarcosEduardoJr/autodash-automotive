# `domain/src/test/java/com/autodash/domain`

Source set de **teste unitário (JVM)**. `ObserveVehicleSpeedTest` prova a regra com um
fake do `CarRepository` — sem device/emulador. É a promessa da Clean Architecture no automotivo (docs/08).
