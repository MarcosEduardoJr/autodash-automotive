# `res/`

Recursos Android do módulo `app`. **Macro:** é aqui que o multi-brand se materializa —
cada *flavor* (marca) pode sobrepor recursos, e a OEM sobrepõe via **RRO** em runtime
(`docs/06`). O subdir `xml/` traz o descriptor automotivo.
