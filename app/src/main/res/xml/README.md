# `res/xml/`

**Micro:** `automotive_app_desc.xml` — declara a **categoria** do app (`template`).

**Macro:** sem esse descriptor + o `uses-feature automotive` no manifest, o sistema não
reconhece o app como automotivo e a Play não o distribui no carro (`docs/08`).
