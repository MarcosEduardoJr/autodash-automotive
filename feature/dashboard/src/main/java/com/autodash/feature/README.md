# `feature/dashboard/src/main/java/com/autodash/feature/`

Segmento de **pacote** (`feature`). Convenção Android: pacote = namespace reverse-domain; este nível só agrupa os pacotes-filhos (o código está nas folhas).
